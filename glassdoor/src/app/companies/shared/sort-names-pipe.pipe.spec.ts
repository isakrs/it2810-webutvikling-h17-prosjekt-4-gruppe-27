import { SortNamesPipePipe } from './sort-names-pipe.pipe';

describe('SortNamesPipePipe', () => {
  it('create an instance', () => {
    const pipe = new SortNamesPipePipe();
    expect(pipe).toBeTruthy();
  });
});
