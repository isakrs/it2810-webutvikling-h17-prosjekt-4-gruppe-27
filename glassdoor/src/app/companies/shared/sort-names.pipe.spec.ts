import { SortNamesPipe } from './sort-names.pipe';

describe('SortNamesPipe', () => {
  it('create an instance', () => {
    const pipe = new SortNamesPipe();
    expect(pipe).toBeTruthy();
  });
});
